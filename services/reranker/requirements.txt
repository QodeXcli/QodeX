# QodeX local reranker service deps.
# Install:  pip install -r requirements.txt
#
# torch provides the MPS (Apple-Silicon GPU) backend; sentence-transformers
# wraps the bge-reranker cross-encoder; fastapi/uvicorn serve the /v1/rerank API.
sentence-transformers>=3.0.0
torch>=2.2.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0
